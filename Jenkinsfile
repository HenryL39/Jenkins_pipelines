pipeline {
	agent any
	stages {
		stage('Sequentiels') {
  			stages {
    			stage ('Sequence 1') {
    				echo "Sequence 1"
    			}

    			stage ('Sequence 2') {
    				echo "Sequence 2"
    			}
  			}
		}
	}
}